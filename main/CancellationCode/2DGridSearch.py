import matplotlib.pyplot as plt
import numpy as np

total = 3120

matrix = [[0 for x in range(total)] for y in range(total)]

increment = 20
holder = [0,0, low]
starti = 0
startq = 0
for index in range(0, 5):

    for i in range(starti, total, increment):

        for q in range(startq, total, increment):
            # get val

    starti = holder[0] - increment
    startq = holder[1] - increment
    total = ho
    increment = increment/index
